<%= link_to("<<back to the list", { :action => 'index' , :subject_id => @subject.id}, :class => 'back-link') %>
<div class="page delete">

<%= form_for( :page , :url => {:action => 'destroy', :id => @page.id , :subject_id => @subject.id}) do |f| %>
<p>are you sure you want to permently delete this object ??????</p>
<p class="reference-name"><%= @page.name %></p>


<div class="form-buttons">
	
	<%= submit_tag("delete page") %>
</div>
<% end %>
</div>

